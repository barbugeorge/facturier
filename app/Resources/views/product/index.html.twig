{# app/Resources/views/product/index.html.twig #}
{% extends 'base.html.twig' %}
    {% block submenu %}
        {{ parent() }}
          <ul class="nav nav-sidebar">
            <li><a href="#" data-toggle="collapse" data-target="#product">Actiuni Produs</a></li>
                <ul id="product" class="collapse">
                    <li class=""><a href="#">Adauga Produs</a></li>
                    <li class=""><a href="#">Editeaza Produs</a></li>
                </ul>
            <li><a href="#">SubLinkB</a></li>
            <li><a href="#">SubLinkC</a></li>
          </ul>
    {% endblock %}


{% block body %}
<h3>Exemplu - afisare date in tabel</h3>
<div class="table-responsive">
    <table class="table table-hover records_list">
        <thead>
            <tr>
            {% for val in entities[0] %}
                <th>{{ val }}</th>
            {% endfor %}
            </tr>
        </thead>
        <tbody>
            {% for entity in entities %}
            <tr>
                {% for val in entity %}
                    <td>{{ val }}</td>
                {% endfor %}
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}